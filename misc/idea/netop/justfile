# netop — network monitor TUI

default:
    @just --list

build:
    cargo build

release:
    cargo build --release

run *args:
    cargo run -- {{args}}

check:
    cargo check

test:
    cargo test

fmt:
    cargo fmt

lint:
    cargo clippy -- -D warnings
